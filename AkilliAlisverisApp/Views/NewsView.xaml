<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:AkilliAlisverisApp.Models"
             xmlns:viewmodels="clr-namespace:AkilliAlisverisApp.ViewModels"
             xmlns:mct="clr-namespace:CommunityToolkit.Maui.Behaviors;assembly=CommunityToolkit.Maui"
             x:Class="AkilliAlisverisApp.Views.NewsView"
             x:DataType="viewmodels:NewsViewModel"
             Title="{Binding Title}">

    <ContentPage.Behaviors>
        <mct:EventToCommandBehavior EventName="Appearing" Command="{Binding LoadNewsCommand}"/>
    </ContentPage.Behaviors>

    <Grid RowDefinitions="Auto, *">
        <SearchBar Grid.Row="0"
                   Placeholder="Haber Ara"
                   Text="{Binding SearchText}"
                   SearchCommand="{Binding LoadNewsCommand}"
                   Margin="10"/>

        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Articles}"
                        SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:NewsArticle">
                    <Frame Margin="10,5" Padding="15" CornerRadius="10" BorderColor="LightGray">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:NewsViewModel}}, Path=GoToDetailCommand}"
                                                  CommandParameter="{Binding .}"/>
                        </Frame.GestureRecognizers>
                        <VerticalStackLayout Spacing="5">
                            <Label Text="{Binding Title}" FontAttributes="Bold" FontSize="18"/>
                            <Label Text="{Binding ShortContent}" FontSize="14" TextColor="Gray"/>
                            <Label Text="{Binding PublishedDate, StringFormat='{0:dd MMMM yyyy}'}"
                                   FontSize="12"
                                   TextColor="DarkGray"
                                   HorizontalOptions="End"/>
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.EmptyView>
                <StackLayout Padding="20" VerticalOptions="Center" HorizontalOptions="Center">
                    <Label Text="Yüklenecek haber bulunamadı." TextColor="Gray"/>
                </StackLayout>
            </CollectionView.EmptyView>
        </CollectionView>

        <ActivityIndicator Grid.RowSpan="2" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" VerticalOptions="Center" HorizontalOptions="Center"/>
    </Grid>
</ContentPage>